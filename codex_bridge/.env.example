# Codex Bridge 配置
# 复制此文件为 .env 并填入实际值: cp .env.example .env

# [必填] Silk 后端地址
BRIDGE_SERVER=localhost:8006

# [必填] Bridge Token（在 Silk 设置页生成；与 cc_bridge 共用同一个 token）
BRIDGE_TOKEN=your-token-here

# [可选] Codex 默认工作目录（默认为当前目录）
# BRIDGE_WORKING_DIR=/workspace/your-project

# [可选] 日志级别: DEBUG / INFO / WARNING / ERROR（默认 INFO）
# BRIDGE_LOG_LEVEL=INFO

# [推荐] 在仓库外创建的 venv 里的 python3 绝对路径
# BRIDGE_PYTHON=/path/to/your-venv/bin/python3

# [可选] WSS 自签证书时设为 1（bridge.sh 会加 --tls-insecure；勿在生产公网长期开启）
# BRIDGE_TLS_INSECURE=1

# ----- Codex 专属选项 -----

# [可选] 默认 1：执行 codex 时带 --dangerously-bypass-approvals-and-sandbox（与 cc_bridge 行为对齐）
# 设为 0 / false 时改用默认沙箱与人审，但 M1 不实现交互审批，模型一旦请求高权限命令将卡住。
# CODEX_AUTO_APPROVE=1

# [可选] 上报给后端 /agent-bridge 的 agentType（一般不改；后端 AgentRegistry 中以此 key 查 CodexDescriptor）
# CODEX_AGENT_TYPE=codex

# ----- Codex CLI 自身的认证 / 模型选择 -----
# Codex CLI 不通过环境变量配置 API key，统一通过 `codex login` 写入 ~/.codex/auth.json
# 如需切换模型 / provider，请编辑 ~/.codex/config.toml 或在启动 bridge 时通过 CODEX_EXTRA_ARGS 透传
# 例如: export CODEX_EXTRA_ARGS="--model gpt-5.2-codex"
# (M1 暂未支持 CODEX_EXTRA_ARGS 透传到 executor，预留 M2 实现)
